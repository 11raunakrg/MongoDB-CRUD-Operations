<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body>
    <div class="w-full h-full bg-zinc-800 text-white p-10">
      <h1 class="text-2xl font-semibold tracking-tight mt-3 mb-5">
        Update User
      </h1>

      <form action="/update/<%= user.id%>" method="post">
        <input
          type="text"
          class="px-3 py-2 bg-transparent border-2 border-zinc-600 rounded outline-none mr-3"
          placeholder="Enter your name"
          name="name"
          value="<%= user.name%>"
        />

        <input
          type="text"
          class="px-3 py-2 bg-transparent border-2 border-zinc-600 rounded outline-none mr-3"
          placeholder="Enter your email"
          name="email"
          value="<%= user.email%>"
        />

        <input
          type="text"
          class="px-3 py-2 bg-transparent border-2 border-zinc-600 rounded outline-none"
          placeholder="Enter image URL"
          name="image"
          value="<%= user.imageUrl%>"
        />

        <input
          type="submit"
          value="Update User"
          class="px-4 py-2 ml-3 bg-green-500 rounded cursor-pointer"
        />
      </form>
    </div>
  </body>
</html>
